// =====================================================================
// options.js — user preferences that persist across match resets (but
// not across page reloads). Imported as a shared mutable singleton.
// =====================================================================

export const gameOptions = {
  edgeScroll: true,
};
